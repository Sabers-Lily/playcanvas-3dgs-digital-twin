{
  "name": "playcanvas-3dgs-digital-twin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "cmd /c pnpm -r --parallel dev",
    "dev:web": "cmd /c pnpm --filter web dev",
    "dev:api": "cmd /c pnpm --filter api dev",
    "desktop:dev": "cmd /c pnpm desktop:build:web && pnpm --filter desktop run dev",
    "desktop:build:web": "cmd /c pnpm --filter web build",
    "desktop:verify": "cmd /c pnpm --filter desktop run verify",
    "desktop:pack": "cmd /c pnpm desktop:build:web && pnpm --filter desktop run pack",
    "desktop:dist": "cmd /c pnpm desktop:build:web && pnpm --filter desktop run dist",
    "build": "cmd /c pnpm -r build",
    "build:web": "cmd /c pnpm --filter web build",
    "build:api": "cmd /c pnpm --filter api build",
    "convert:ply": "node scripts/convert-ply-to-sog.mjs"
  }
}
